{
  "_comment": "Manual data overlay — currently EMPTY (pure live mode). Front-end merges these into live AA data only when the model is not yet indexed by Artificial Analysis. To add a model, append to models[] and the overlay auto-retires once AA indexes it (matched by slug/name).",
  "models": []
}
